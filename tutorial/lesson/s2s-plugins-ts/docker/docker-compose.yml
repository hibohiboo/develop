version: '3'
services:
  babel:
    build: ./babel
    volumes:
      - ../packages:/app/packages

  # ソースコードプロトタイピング
  s2s:
    build: ./s2s
    volumes:
      - ./config/s2s/s2s.config.js:/app/s2s.config.js
      - ../packages/babel-plugin-test:/app/babel-plugin-test
      - ../test/src:/app/src
    command: [yarn, run, s2s]

  # ビルドツール
  ts:
    build: ./ts
    volumes:
      - ./config/ts/.babelrc:/app/.babelrc
      - ./config/ts/tsconfig.json:/app/tsconfig.json
      - ./config/ts/tslint.json:/app/tslint.json
      - ./config/ts/jest.config.json:/app/jest.config.json
      - ../src:/app/src
      - ../packages:/app/dist
