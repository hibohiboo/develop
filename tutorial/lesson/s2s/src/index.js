/*
 * @flow
 * Welcome to s2s https://github.com/akameco/s2s.
 * This example is very small. However Useful :)
 * This plugin is that initializing variable with flow information. test
*/

type User = { id: number, name: string }
// type `var user: User` and save! test ttt
var user: User = { id: 0, name: '' }
