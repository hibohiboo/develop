#
# site.yml
#
- hosts: localhost
  become: yes # 
  become_user: root # 以下、sudoコマンドが不要になる 
  roles:
    - common
    - git
    - docker
    - andrewrothstein.kubectl
    - andrewrothstein.minikube
    # - node
    # - vim
    # - aws